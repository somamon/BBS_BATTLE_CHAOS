# ---- アプリ ----
APP_ENV=production
APP_URL=http://localhost:18723   # メール確認リンク等の絶対URL生成に使用

# ---- ポート ----
NGINX_PORT=18723       # ブラウザ → http://localhost:${NGINX_PORT}
DB_PORT=13306          # 外部DBクライアント → localhost:${DB_PORT}

# ---- データベース ----
DB_NAME=bbs
DB_USER=bbs
DB_PASSWORD=
DB_ROOT_PASSWORD=