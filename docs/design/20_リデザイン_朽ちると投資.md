# リデザイン: 朽ちる掲示板と投資経済（株式モデル）

> このドキュメントが新しいコア設計の正。旧設計（02〜07 の 汚染/変異/天候/world_states/events/challenges）は
> 本設計を優先し、当面は採用しない（将来のフレーバーとして保留）。
> 主要メカニクスはすべて確定済み（§2〜§5）。残課題は §9。

## 1. コンセプト

**2層構造**で成り立つ。

```
┌─ 投資家層（登録ユーザーのみ）────────────────┐
│  お金(初期500)で「スレッドの株」を売買し、配当と    │
│  株評価益で総資産を競う。                          │
└──────────────────▲─────────────────┘
                       │ 賑わいが「将来性シグナル」になる
┌─ 匿名掲示板層（誰でも）──┴─────────────────┐
│  匿名でスレ立て・レス投稿（お金不要・報酬なし）。     │
│  スレもレスも時間で朽ちて消える。                   │
└──────────────────────────────────┘
```

3つの概念:

1. **朽ちる** — スレもレスも個別に HP を持ち、時間で減り、0 で消滅。
2. **投資** — 登録ユーザーがスレッドの株を買う。お金の50%がそのスレのHP回復に充たり延命される。
3. **お金** — 登録時に 500。レス報酬は無し。**自分が株主のスレに新規投資が入ると配当**で増える。

**ゴール（競争）**: 総資産（所持金 ＋ 保有株の評価額）ランキングで上位を狙う。

---

## 2. 主要メカニクス

### 2.1 遅延減衰（Lazy Decay）— 実装の肝

cron で全行を減らさず、**読むたびに経過時間から現在 HP を計算**する。

```
現在HP = max(0, stored_hp − decay_per_min × 経過分数)
経過分数 = (now − last_decay_at) ÷ 60秒
```

- `stored_hp` / `last_decay_at` は「HP を確定させた時刻のスナップショット」。
- 投資など HP が変わるイベント時に「現在HPを算出 → stored_hp に書き戻し → last_decay_at=now」で確定。
- 表示時は計算だけ（書き込み不要）。
- 現在HP ≤ 0 で `status='dead'`。dead は一覧から除外、**復活不可**。

### 2.2 投資（株式モデル）— 経済の心臓

登録ユーザーがスレッドに `amount`（≥1、所持金以下）を投資すると:

| 配分 | 割合 | 行き先 |
|---|---|---|
| HP回復 | **50%** | 対象スレの `hp += 0.5 × amount`（`max_hp` で上限、超過分は sink へ） |
| 配当 | **30%** | **既存株主**へ持ち分比率で分配（投資者本人は対象外） |
| 消滅(sink) | **20%** | 経済から消える（インフレ抑制） |

- 投資者は **`amount` 株**を受け取る（`holdings` に加算、`thread.total_shares += amount`）。
- 配当は「新規株を発行する**前**の既存株主」に配る。**最初の投資者は既存株主が居ない**ので、その回の配当(30%)は sink に合流。
- dead のスレには投資不可。
- 全工程を**1トランザクション**（出金・配当入金・HP更新・株発行・ログ）で行い、失敗時ロールバック。

**穴①②がこれで解ける:**
- 他人に投資する動機 = 自分が株主になり、**後続投資の配当(30%)**を得る（早期参入が得）。
- 「貯め込み最強」問題の解消 = 投資しないと配当ストリームに乗れない。

### 2.3 株の評価額

```
保有株の評価額 = (my_shares ÷ thread.total_shares) × スレの現在HP
```

HP は時間で溶けるので、**賑わいが止まれば評価額も配当も枯れ、dead で株は紙くず**。
「伸びるうちに乗って、朽ちる前に配当を回収」する投機ゲームになる。

### 2.4 お金の出入りまとめ

| 増える | 減る |
|---|---|
| 登録時 +500 | スレへの投資 −amount |
| 自分が株主のスレに新規投資 → 配当(投資額×30%×持ち分比率) | （sink 20% と HP化 50% で経済から退場） |

レス投稿・スレ作成は**無料・無報酬**（匿名掲示板行為）。

---

## 3. バランス定数（`App\Config\Game` に集約）

| 定数 | 値 | 意味 |
|---|---|---|
| `INITIAL_MONEY` | 500 | 登録時の所持金 |
| `SPLIT_HP` / `SPLIT_DIVIDEND` / `SPLIT_SINK` | 0.50 / 0.30 / 0.20 | 投資額の配分 |
| `MIN_INVEST` | 1 | 最低投資額 |
| `THREAD_INIT_HP` | 300 | スレ初期HP |
| `THREAD_MAX_HP` | 1000 | スレHP上限 |
| `THREAD_DECAY_PER_MIN` | 5 | スレ減衰（放置1時間で −300） |
| `POST_INIT_HP` | 100 | レス初期HP |
| `POST_DECAY_PER_MIN` | 5 | レス減衰（放置20分で消滅） |

> レスは投資対象外なので `max_hp` は持たず、`POST_INIT_HP` から減るのみ。

---

## 4. DB 設計（MySQL 8.0）

> 旧 `threads/posts` は作り直す（汚染/変異/天候を廃止、耐久スナップショットと株情報を追加）。

### users（登録投資家）

| カラム | 型 | NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | VARCHAR(26) | NO | — | ULID (PK) |
| email | VARCHAR(255) | NO | — | ログインID（一意） |
| name | VARCHAR(50) | NO | — | 表示名（ランキング表示用） |
| password_hash | VARCHAR(255) | NO | — | `password_hash()` の出力 |
| money | INT | NO | 500 | 所持金 |
| created_at | DATETIME | NO | — | |

**索引**: `uniq_users_email (email) UNIQUE`（メールでログインするため一意）

### threads

| カラム | 型 | NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | VARCHAR(26) | NO | — | ULID (PK) |
| creator_id | VARCHAR(26) | YES | NULL | 立てた登録ユーザー（匿名は NULL、表示用） |
| title | VARCHAR(255) | NO | — | タイトル |
| hp | INT | NO | 300 | 確定HP（スナップショット） |
| max_hp | INT | NO | 1000 | HP上限 |
| decay_per_min | INT | NO | 5 | 減衰レート |
| total_shares | INT | NO | 0 | 発行済み総株数 |
| last_decay_at | DATETIME | NO | — | HP確定時刻 |
| status | VARCHAR(10) | NO | 'alive' | alive / dead |
| post_count | INT | NO | 0 | レス数（表示・勢いの目安） |
| created_at | DATETIME | NO | — | |
| updated_at | DATETIME | NO | — | |

**索引**: `idx_threads_status_created (status, created_at)`

### posts（レス・投資対象外）

| カラム | 型 | NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | VARCHAR(26) | NO | — | ULID (PK) |
| thread_id | VARCHAR(26) | NO | — | FK threads |
| author_hash | VARCHAR(64) | NO | — | 匿名識別（IPハッシュ） |
| author_id | VARCHAR(26) | YES | NULL | 登録ユーザーなら紐付け（表示用） |
| content | TEXT | NO | — | 本文 |
| hp | INT | NO | 100 | 確定HP |
| decay_per_min | INT | NO | 5 | 減衰レート |
| last_decay_at | DATETIME | NO | — | HP確定時刻 |
| status | VARCHAR(10) | NO | 'alive' | alive / dead |
| created_at | DATETIME | NO | — | |

**索引**: `idx_posts_thread (thread_id, status)`

### holdings（持ち分）

| カラム | 型 | NULL | 説明 |
|---|---|---|---|
| user_id | VARCHAR(26) | NO | 株主 (FK users) |
| thread_id | VARCHAR(26) | NO | 対象スレ (FK threads) |
| shares | INT | NO | 保有株数 |

**PK**: (user_id, thread_id) / **索引**: `idx_holdings_thread (thread_id)`

### investments（監査ログ）

| カラム | 型 | NULL | 説明 |
|---|---|---|---|
| id | VARCHAR(26) | NO | ULID (PK) |
| investor_id | VARCHAR(26) | NO | 投資者 (FK users) |
| thread_id | VARCHAR(26) | NO | 対象スレ |
| amount | INT | NO | 投資総額 |
| to_hp | INT | NO | HPへ充当した額 |
| to_dividend | INT | NO | 配当に回した額 |
| to_sink | INT | NO | 消滅額 |
| created_at | DATETIME | NO | |

**索引**: `idx_investments_investor (investor_id)`, `idx_investments_thread (thread_id)`

---

## 5. ユースケース

| UC | 認証 | 処理 | 失敗条件 |
|---|---|---|---|
| ユーザー登録 | 不要→登録 | email + name + password 受取 → `password_hash()` で保存 → users 作成（money=500）→ セッションに紐付け | メール重複 / 形式不正 |
| ログイン | 不要 | email で引き → `password_verify()` → セッションに user_id を保存 | メール不在 / パス不一致 |
| スレ作成 | 匿名可 | thread 作成（HP満タン, total_shares=0）。登録者なら creator_id 記録 | — |
| レス投稿 | 匿名可 | スレ alive 確認 → post 作成 → post_count++ | スレ dead |
| 投資 | **要登録** | 残高・alive 確認 → §2.2 を1トランザクションで実行 | 未登録 / 残高不足 / dead / amount<MIN |
| 一覧/詳細 | 匿名可 | 現在HPを遅延計算して表示（dead 除外） | — |
| ランキング | 匿名可 | 全ユーザーの `money + Σ株評価額` を集計し降順 | — |
| マイページ | 要登録 | 所持金・保有株・各評価額・含み損益 | 未登録 |

---

## 6. 画面 / ルート

| メソッド | パス | 画面 | 認証 |
|---|---|---|---|
| GET | /threads | スレ一覧（HPバー・勢い・時価総額） | 匿名可 |
| GET | /thread/{id} | スレ詳細＋レス＋投資パネル | 匿名可 |
| POST | /threads | スレ作成 | 匿名可 |
| POST | /thread/{id}/posts | レス投稿 | 匿名可 |
| POST | /thread/{id}/invest | 投資（amount） | 要登録 |
| GET/POST | /register | ユーザー登録 | — |
| GET | /me | マイページ（所持金・保有株） | 要登録 |
| GET | /ranking | 資産ランキング | 匿名可 |

ヘッダー: 未登録なら「登録して投資」ボタン、登録済みなら所持金を常時表示。POST 系は CSRF 必須。

---

## 7. アーキテクチャ方針

簡易MVC（`App\Model` + `Presentation/Controller|View`）を踏襲。
- 減衰計算は Model に集約（`Thread::currentHp()` / `decayAndPersist()`）し二重化を防ぐ。
- 投資ロジックは1つのユースケース（`InvestService::invest()`）にまとめ、トランザクション境界をそこに置く。
- ゲーム定数は `App\Config\Game` に一元化。
- ログインは email + password（`password_hash()` / `password_verify()`）。セッションに user_id を保持。

---

## 8. 経済特性（重要な留意点）

レス報酬を 0、配当は再分配（新規お金を生まない）にしたため、**お金は「初期500×登録人数」が天井で、投資のたびに 70%（HP化50%＋sink20%）が循環マネーから退場する＝デフレ**。

➡ **このゲームは構造上「有限ラウンド」**（採用確定／蛇口は足さない）。お金が枯れると新規投資が止まり、全スレが朽ちて終局へ向かう。

**終局条件（どちらか）**:
- 全スレッドが `dead`、または
- 全登録ユーザーの所持金合計 < `MIN_INVEST`（＝誰も投資できない）

**終局処理**: ランキングを確定し「リザルト画面」で勝者（総資産トップ）を表示。以降は新ラウンド開始（全データをアーカイブ→初期化）まで閲覧のみ。
※ 終局判定は投資/閲覧のたびに軽くチェックする遅延方式でよい（cron 不要）。

---

## 9. 残課題

| # | 項目 | 状態 |
|---|---|---|
| 1 | 有限ラウンド or 無限 | ✅ **有限ラウンド**で確定（§8、蛇口なし） |
| 2 | ログイン方式 | ✅ **メールアドレス＋パスワード**で確定（§4 users / §5） |
| 3 | 数値バランス（減衰・初期HP・配分%） | 🔧 実プレイ後に調整。`App\Config\Game` で集中管理 |
| 4 | スレ作成のスパム対策 | 🔧 実装後に判断（クールダウン等）。MVP は無対策 |
| 5 | レス延命の UI（勢いの見せ方） | 🔧 実装時に検討（レス数・残HPバー等） |

→ 残るは実装時チューニング項目のみ。**コア設計は確定**。次は実装（スキーマ刷新 → Model/UseCase → Controller/View）。
