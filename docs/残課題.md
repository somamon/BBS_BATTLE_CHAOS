# 残課題（現状バックログ）

> 作成日: 2026-06-07 / 対象ブランチ: feature/develop1
> 関連: 詳細な判定経緯は [リリース判定_課題.md](リリース判定_課題.md) を参照。

## 現状サマリ

- 🔴 ブロッカー B1〜B4：**解消済み**（メール実送信/本番ガード/HTTPS/マイグレーションランナー）
- 🟠 高 H1〜H6：**解消済み**（N+1/スロットル/ボット資金補充/入力長検証/CI・結合テスト/期限切れ掃除）
- 🟡 中 M1〜M5：**実装済み**（パスワードリセット/ラウンド制/バランスのenv化/構造化ログ/法務・退会・年齢確認・表現）
- テスト：ユニット 100 件 ＋ 結合 1 件 グリーン
- リリース可否：**クローズドβは可**。一般公開は下記の**残作業**（法務テキスト確定・βでのバランス実調整）を済ませてから推奨。

残るは M5 の法務テキスト確定など下記「M1〜M5の残作業」、⚪低、および直近実装の検討事項。

---

## 🟡 中（一般公開前に対応推奨）— **実装済み**

### M1. パスワードリセット ✅
- 申請（`/password/forgot`）→ メール → 新パスワード設定（`/password/reset?token=`）のフローを追加。
- 確認メール基盤と同設計：生トークンはメールのみ、DBはSHA-256ハッシュ。有効期間1時間・使い捨て・列挙対策（一律応答）。
- 実装: `PasswordReset`(entity) / `PasswordResetRepository` / `RequestPasswordReset` / `ResetPassword` / `PasswordResetMailSender`、migration `012`。ログインに「パスワードをお忘れですか？」導線。

### M2. 終局後のリセット（ラウンド制）✅
- 終局→ランキング確定→初期化→新ラウンド開始のサイクルを実装。
- 実装: `rounds`/`round_rankings`(migration `013`/`014`) / `Round`(entity) / `RoundRepository` / `FinalizeAndResetRound` / `GameStateResetter`。
- 実行は **CLI 専用** `php bin/round.php [--force]`（公開GETでの誤発火防止）。結果ページにラウンド番号を表示。
- 運用手順: [運用_ラウンドとログ.md](運用_ラウンドとログ.md)。

### M3. ゲームバランス（env 上書き＋文書化）✅
- 主要パラメータ（株価係数/配分/減衰/レベル閾値/ボット挙動/人口減衰係数）を `GAME_*` 環境変数で**再ビルド無し**に上書き可能化。未設定なら従来の既定値。
- 実装: `App\Config\Game` をアクセサ化、呼び出し側を更新。`.env.example` と [ゲームバランス調整.md](ゲームバランス調整.md) に一覧。
- ※ 実数値の調整自体は**βでの計測後**に行う（このタスクの範囲外）。

### M4. 観測性（構造化ログ・相関ID・KPI）✅
- `error_log` の文字列ログを JSON 1行/イベントの構造化ログへ。全行に相関ID（`request_id`）。
- KPIイベント: `user_registered` / `investment_made`（人間/NPC区別）/ `request`（status・duration→エラー率/レイテンシ）/ `round_reset`。
- レスポンスに `X-Request-Id`、上流の同ヘッダを引継ぎ。実装: `Logger`(port) / `JsonLogger` / `RequestContext`。
- 死活監視・アラートは集約基盤側で設定（[運用_ラウンドとログ.md](運用_ラウンドとログ.md)）。

### M5. 法務・コンプライアンス ✅
- 利用規約 `/terms`・プライバシーポリシー `/privacy`（日英・**確定版本文**、全ページにフッター導線）。運営者名・連絡先のみ `LEGAL_OPERATOR` / `LEGAL_CONTACT`（未設定なら `MAIL_FROM`）で差し替え。
- 退会（データ削除）`/account/delete`：投資ログ・保有株・本人ユーザーを削除（匿名投稿は本人紐付けのみ除去して残す）。マイページに導線。
- 登録時に**年齢確認＋規約・プライバシー同意**チェックを必須化。
- 「お金を**賭ける**」表現を「投資する／見極める」へ言い換え（ja/en）。フッターに「ゲームであり換金不可」の明示。
- 実装: `LegalController` / `AccountController` / `DeleteAccount`、各ビュー・i18n。

---

## M1〜M5 の残作業（公開前に人手で確定すべきもの）

- **M5 法務**：本文は確定版（全年齢対象・広告(忍者AdMax)対応・東京地裁管轄・利用拒否権/ゲーム仕様変更権/通報対応/AI大量投稿禁止を反映）。連絡先既定は `8556iamsmartphone0124@gmail.com`、運営者名既定は「BBS BATTLE CHAOS 運営チーム」。実体に合わせる場合は `LEGAL_CONTACT` / `LEGAL_OPERATOR` を設定。
- **M3 バランス実数値**はβ計測後に `GAME_*` で調整（N1インフレ対策の主レバーは `GAME_BOT_REFILL_TO`）。
- **M2 ラウンド運用**：自動切替するなら cron 登録（または手動運用）を決める。`bin/round.php` を本番DBへ誤実行しない運用ガードを徹底。
- **M4 監視**：ログ集約基盤での KPI ダッシュボード／エラー率アラートの設定。

---

## ⚪ 低

- **L1. セッションがファイル保存**: 単一ホスト前提。水平スケール時は共有ストア（Redis 等）へ。
- **L2. ログインで `password_needs_rehash` 未対応**: 将来 bcrypt コスト変更時に旧ハッシュが残る。
- **L3. CSP が `style-src 'unsafe-inline'`**: インラインstyle依存。将来 nonce 化の余地。
- **L4. ライセンス/メタ未設定**: `composer.json` に license 等が無い。

---

## 直近の実装で新たに出た検討事項（NEW）

### N1. ボット資金補充によるインフレ
- H3 で資金切れボットを `BOT_REFILL_TO` まで補充＝**通貨が増え続ける**。長期運用で株価・評価額が膨張し得る。
- 対応案: 補充に上限/減衰を設ける、ラウンド制（M2）と連動。
- ※ランキング歪み（NPCが上位を占める懸念）は **`RankingQuery` でNPC(is_bot=1)を除外**して解消済み。順位は人間プレイヤー限定。

### N2. 開発時アクセスの導線
- HTTP(80)→HTTPS(443) リダイレクトは `$host` ベースのため、開発のポート（18723→18443）ではリダイレクト先のポートが落ちる。
- 運用上の正解（標準443）では問題なし。開発は **https://localhost:18443 へ直接**アクセス（自己署名のため警告承認）。READMEに明記推奨。

### N3. 本番TLS証明書の差し替え運用
- 開発は自己署名（`docker/nginx/certs/`、gitignore）。本番は実証明書のマウント or 終端を LB/プロキシで行う運用手順を文書化する。

### N4. 結合テストは破壊的
- `DatabaseFlowTest` は人間ユーザーと取引データを TRUNCATE/DELETE する（`RUN_DB_TESTS=1` 時のみ実行）。**使い捨てDB専用**。誤って本番/共有DBへ向けない運用ガードを明記。

### N5. MarketSimulator が保守処理も担う
- 期限切れ掃除（H6）を「占有された tick」に相乗りさせている（cron不要のため）。責務がやや混在。
- 将来 cron/ワーカーを導入するなら、掃除はそちらへ移すのが綺麗。

### N6. Database::connect に DB ポート指定が無い
- ホストからマッピングポート（13306）へ直接接続できない（DSN がポート未指定＝3306固定）。コンテナ内実行は問題なし。必要なら `DB_PORT` 対応を追加。

---

## 推奨対応順（一般公開へ）

> M1〜M5 は実装完了。残るは下記の人手確定とβ運用。

1. **M5 法務テキストの確定**（雛形→専門家レビュー版へ差し替え、運営者情報/連絡先の記載）— 公開の前提
2. **βでの計測 → M3 バランス実調整**（`GAME_*`）／**N1 インフレ対策**（`GAME_BOT_REFILL_TO` 等。人間限定ランキングは対応済み）
3. **M2 ラウンド運用の確定**（cron 自動 or 手動）／**M4 監視ダッシュボード・アラート設定**
4. ⚪低（L1〜L4）・N2〜N6 を順次
